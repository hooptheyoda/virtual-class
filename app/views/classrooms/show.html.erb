<h1>Classroom</h1>
<p>Subject: <%= @classroom.subject %></p>
<p>Teacher: <%= User.find(@classroom.teacher_id).full_name %></p>
<h3>Schedules: </h3>
<ul>
  <% @classroom.classroom_schedules.each do |s| %>
  <li>
    <%= $weekdayname[s.weekday] %> --
  </li>
  <% end %>
</ul>

<h3>New Schedule: </h3>
<%= form_with(model: ClassroomSchedule.new, local: true) do |f| %>
<%= hidden_field_tag :classroom_id, @classroom.id %>
<%= f.label :weekday %><br>
<%= f.select :weekday, $weekdayname.map.with_index{|x,i| x=[x,i]} %>
<br>
<%= f.label "from: " %><br>
<%= f.time_select :from, {minute_step: 60} %> to
<%= f.time_select :timeto, {minute_step: 60} %>
<br>
<%= f.submit "Add" %><br>
<% end %>
